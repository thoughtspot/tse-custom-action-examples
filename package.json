{
  "name": "tse-custom-action-examples",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@thoughtspot/visual-embed-sdk": "^1.31.0",
    "react": "17.0.2",
    "react-burger-menu": "3.0.6",
    "react-dom": "17.0.2",
    "react-router-dom": "6.2.2",
    "react-scripts": "4.0.0",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "NODE_OPTIONS=--openssl-legacy-provider PORT=8000 react-scripts start",
    "build": "react-scripts NODE_OPTIONS=--openssl-legacy-provider build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
